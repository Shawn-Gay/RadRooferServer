[build]
builder = "nixpacks"
buildCommand = "dotnet publish src/RadRoofer.Api/RadRoofer.Api.csproj -c Release -o out"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["dotnet-sdk_9"]

[deploy]
startCommand = "dotnet out/RadRoofer.Api.dll"
